name: Build and Release Electron App

on:
  push:
    tags:
      - "v*.*.*" # Es. push di un tag v1.0.0
  workflow_dispatch: # Permette di lanciarla manualmente dal sito GitHub

jobs:
  build:
    runs-on: windows-latest
    # Necessario per permettere alla action di creare una Release e fare upload dell'exe
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Create .env.electron from secrets
        run: |
          echo "${{ secrets.ENV_ELECTRON_FILE }}" > .env.electron

      - name: Build and Publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run electron:build:win -- -p always
